{
  "name": "medconnect-functions",
  "description": "Cloud Functions MedConnect (authLookup, claimPatientAccount, setVerifiedClientType) — chantiers 6/7 v2.9.42",
  "private": true,
  "type": "commonjs",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "lint": "node -e \"require('./index.js') && console.log('functions: chargement OK')\"",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0",
    "web-push": "^3.6.7"
  }
}
