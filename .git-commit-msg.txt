fix(mobile + échange) : listeners branchés, double-fetch corrigé, defer, SDK hors-ligne

Communication mobile ↔ desktop (sous conditions d'abonnement desktop) :
- app.js : ExchangeBridge.startRoleListeners enfin branché au login —
  il existait mais n'était appelé NULLE PART : le desktop écrivait,
  le mobile ne recevait jamais. Première émission = état initial
  silencieux, suivantes = notification discrète + rafraîchissement
  de la section concernée. Écritures toujours gatées (deux vitesses).
- hospitals_registry.js : listeners relancés au changement
  d'établissement actif (les filtres hospitalId en dépendent)
- hospital-lab.js : bascule labOrders → labRequests/labResults du
  contrat existant (dérive de schéma évitée) ; statuts alignés sur
  le listener infirmier mobile ; à la saisie du résultat, émission
  d'un labResults vers le médecin demandeur (listener mobile)
- exchange-bridge.js : beds/admissions/aiQueries reconnus au contrat
- firestore.rules : labOrders retirée (abandonnée) ; lecture de
  labRequests élargie au personnel clinique vérifié (les requêtes
  hôpital-entier du desktop et du listener infirmier étaient
  impossibles avec canReadMedicalData seul)

Bugs version mobile (documentés, autorisés à corriger) :
- db.js : double-fetch corrigé — les 12 collections couvertes par un
  listener temps réel sont exclues du .get() initial (la première
  émission onSnapshot livre les mêmes données) : ~2x moins de
  lectures Firestore au démarrage ; 'users' conservé (listener
  filtré, sous-ensemble)
- index.html : defer sur les 43 scripts externes (SDK compris) —
  le parsing/rendu initial n'est plus bloqué, ordre préservé
- sw.js v3.4 : SDK Firebase ajouté au précache (réponse opaque
  rejetée par le cache dynamique → la PWA hors-ligne cassait)

Nettoyage : titre de section des règles corrigé, aucune référence
labOrders résiduelle, zéro console.log de debug constaté.
